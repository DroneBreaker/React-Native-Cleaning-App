import React from 'react'
import { Box, Center, Text } from 'native-base'

export default function Profile() {
  return (
    <Box h={"100%"} px="10" py={35}>
        <Center mt={40}>
            <Text fontWeight={"semibold"} fontSize={25}>Profile yet to be built...</Text>
        </Center>
    </Box>
  )
}