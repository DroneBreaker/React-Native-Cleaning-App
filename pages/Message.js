import React from 'react'
import { Box, Center, Text } from 'native-base'

export default function Message() {
  return (
    <Box px="10" py={35}>
        <Center mt={40}>
            <Text fontWeight={"semibold"} fontSize="lg">Message page on the way...</Text>
        </Center>
        
    </Box>
  )
}